namespace general
{
    var listaVehiculos:Array<Vehiculo> = new Array<Vehiculo>();
    export function agregar()
    {
        var marca:string;
        var modelo:string;
        var precio:number;
        var id:number;

        //logica, si es auto o camioneta
        if(Auto )// tipo == Auto)
        {
            var cantidadPuertas:number;
            nuevoAuto:Auto = new Auto(id, marca, modelo, precio, cantidadPuertas);
        }
        if(Camioneta )// tipo == Auto)
        {
            var cuatroXcuatro:string;

        }
        
    }

    export function eliminar()
    {

    }
}